type ItemMenuProps = {
  image: string;
  title: string;
  description: string;
  price: number;
};

type WhyItemProps = {
  image: string;
  title: string;
  description: string;
};

type EvidenceProps = {
  label: string;
  description: string;
};
